# Core Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
# uvloop>=0.19.0  # Skip on Windows - not supported
httptools>=0.6.1

# Database
motor==3.6.0

# Caching
redis==5.2.1

# Data Validation
pydantic[email]==2.11.5
pydantic-settings==2.6.1

# Authentication & Security
python-jose[cryptography]==3.3.0
PyJWT==2.10.1
cryptography==46.0.5
pycryptodome==3.20.0

# End-to-End Encryption (E2EE)
# signal-protocol==0.2.2  # Requires Rust compiler - commented out for basic deployment
protobuf==5.28.3

# File Handling
python-multipart==0.0.20
aiofiles==24.1.0

# HTTP Client - with HTTP/2 support
httpx[http2]>=0.28.1
h2>=4.1.0

# Configuration
python-dotenv==1.0.1

# QR Code Generation
qrcode[pil]==8.0
Pillow>=10.0.0

# CRITICAL FIX: Added dependencies for Flutter Linux build compatibility
email-validator>=2.0.0
python-dateutil>=2.8.0

# E2EE Advanced Features
libnacl>=1.8.1  # libsodium bindings for additional crypto
PyNaCl>=1.5.0  # PyNaCl for X25519 operations
hkdf>=0.0.3  # HKDF key derivation

# Voice & Video Call Encryption
# aiortc>=1.8.0  # WebRTC implementation - Requires complex dependencies
# av>=12.3.0  # Audio/Video frame handling - Requires ffmpeg

# Message Queue for offline delivery
celery>=5.4.0  # Async task queue
kombu>=5.4.0  # Messaging library

# Backup & Restore
boto3>=1.36.0  # AWS S3 integration (alternative to S3 client)
python-gnupg>=0.5.2  # GPG encryption for backups

# Monitoring & Observability  
prometheus-client>=0.20.0  # Prometheus metrics
opentelemetry-api>=1.27.0  # OpenTelemetry tracing
opentelemetry-sdk>=1.27.0  # OpenTelemetry SDK

# Rate Limiting & Throttling
limits>=3.10.0  # Rate limiting library

# Async HTTP Server for WebSocket
websockets>=14.1  # WebSocket server

# Security & Intrusion Detection
# defender>=20.0.0  # Security analysis (if available) - Package not found

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
requests>=2.31.0
mongomock>=4.1.2