events {}
http {
  upstream backend {
    server backend:8000;
  }
  server {
    listen 80;
    location /health { return 200 "healthy\n"; }
    location / { proxy_pass http://backend; proxy_set_header Host $host; }
  }
}
